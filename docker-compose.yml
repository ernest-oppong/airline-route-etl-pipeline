version: "3.9"

# Portfolio placeholder.
# This file is intentionally lightweight and should be adapted
# to your local stack if you later decide to containerize Hadoop/Hive/NiFi/Kafka.

services:
  app:
    image: python:3.11-slim
    container_name: airline-route-etl-helper
    working_dir: /workspace
    command: ["bash", "-lc", "sleep infinity"]
    volumes:
      - ./:/workspace
