# GitHub Project Description

End-to-end batch ETL pipeline that ingests airline, airport, and route data into HDFS, transforms it into Parquet-based Hive tables, and publishes country-level route aggregations to Kafka through Apache NiFi.
