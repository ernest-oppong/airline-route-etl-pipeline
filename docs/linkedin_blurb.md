# LinkedIn Blurb

Built an end-to-end data engineering pipeline using HDFS, Hive, Apache NiFi, and Kafka. The project ingests raw aviation data, transforms it into Parquet-based analytical tables, and publishes aggregated JSON outputs for downstream consumption.
